# Drop this file into .github/workflows/ — that's it.
# Auto-discovers all *.pactspec.json files in your repo.

name: PactSpec

on:
  push:
    paths: ['**/*.pactspec.json']
  pull_request:
    paths: ['**/*.pactspec.json']

jobs:
  pactspec:
    name: Validate & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: Grumpy254/pactspec/github-action@main
        # Zero config — auto-discovers .pactspec.json files, validates, and tests all skills.
        # To publish on merge to main, uncomment:
        # with:
        #   publish: 'true'
        #   agent-id: ${{ secrets.PACTSPEC_AGENT_ID }}
        #   publisher-key: ${{ secrets.PACTSPEC_PUBLISHER_KEY }}
