{
  "name": "@pactspec/cli",
  "version": "0.1.6",
  "description": "Official CLI for PactSpec — init, validate, publish, verify",
  "bin": {
    "pactspec": "dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && cp src/schema.json dist/schema.json",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
