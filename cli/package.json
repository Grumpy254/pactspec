{
  "name": "@pactspec/cli",
  "version": "0.1.6",
  "description": "Official CLI for PactSpec — init, validate, publish, verify",
  "bin": {
    "pactspec": "dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && cp src/schema.json dist/schema.json",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.8.2"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Grumpy254/pactspec.git",
    "directory": "cli"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["pactspec", "agent", "ai", "mcp", "cli", "validation", "registry"],
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
